<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ycc.mapper.ClazzMapper">

    <select id="list" resultType="org.ycc.pojo.Clazz">
        select
        c.*,
        e.name as masterName,
        (case
        when current_date() &lt; c.begin_date then '未开班'
        when current_date() &gt; c.end_date then '已结课'
        else '在读'
        end) as status
        from clazz c
        left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>
        order by c.update_time desc
    </select>
    <select id="countStudentDegreeData" resultType="java.util.Map">
        select
        c.name as clazzName,
        count(s.id) as studentCount
        from clazz c
        left join student s on c.id = s.clazz_id
        group by c.id, c.name
        order by studentCount desc
    </select>

</mapper>